"use client";
import { Mail, MapPin, MessageSquare } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { PageHero } from "@/components/site-shell";
import { Button, Card, Field, Input, Textarea } from "@/components/ui";
import { api } from "@/services/api";
export default function ContactPage(){const [form,setForm]=useState({name:"",email:"",subject:"",message:""});const[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);try{await api.post("/contact",form);toast.success("Thanks—your message is in our inbox");setForm({name:"",email:"",subject:"",message:""})}catch(error){toast.error(error instanceof Error?error.message:"Could not send message")}finally{setLoading(false)}};return <><PageHero eyebrow="Contact" title="Let’s solve something useful" description="Questions, feedback, partnerships, or product support—send us a message and our team will respond."/><div className="container grid items-start gap-7 py-12 lg:grid-cols-[.7fr_1.3fr]"><div className="space-y-4">{[[Mail,"Email","hello@intellihub.ai"],[MessageSquare,"Support","Visit the help center for common workflows."],[MapPin,"Availability","Remote team · Support Monday–Friday"]].map(([Icon,title,copy])=><Card key={title as string} className="flex gap-4 p-5"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-lavender text-brand"><Icon/></span><div><strong>{title as string}</strong><p className="mt-1 text-sm text-slate-500">{copy as string}</p></div></Card>)}</div><Card className="p-6 md:p-8"><form onSubmit={submit} className="grid gap-5 sm:grid-cols-2"><Field label="Name"><Input required minLength={2} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Email"><Input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><div className="sm:col-span-2"><Field label="Subject"><Input required minLength={3} value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/></Field></div><div className="sm:col-span-2"><Field label="Message"><Textarea required minLength={10} value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></Field></div><div className="sm:col-span-2"><Button loading={loading}>Send message</Button></div></form></Card></div></>}
