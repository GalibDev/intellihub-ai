import { PageHero } from "@/components/site-shell";
const sections=[["Information we collect","We store account details, tools you create, favorites, reviews, conversation messages, recommendation feedback, and saved generations needed to provide the workspace. Uploaded documents are processed in memory and are not stored as files by IntelliHub."],["How information is used","Data is used to authenticate your account, provide requested AI workflows, personalize recommendations, prevent abuse, and improve reliability. Recent conversation context is deliberately limited before it is sent to the configured AI provider."],["Security and retention","Passwords are salted and hashed. Authentication uses secure HTTP-only cookies. Server routes validate input, enforce ownership, and rate-limit requests. You may delete conversations, reviews, favorites, generated tools, and your own content."],["Third-party processors","Gemini processes prompts required for AI features. Google processes identity data only when you choose Google sign-in. MongoDB stores application records. Each provider’s terms and privacy practices also apply."],["Your choices","You can update profile preferences, remove saved data through the product, or contact privacy@intellihub.ai for access or deletion requests."]];
export default function PrivacyPage(){return <><PageHero eyebrow="Legal" title="Privacy policy" description="A plain-language overview of how IntelliHub handles workspace and AI interaction data."/><div className="container max-w-3xl py-12"><p className="text-sm text-slate-400">Effective July 18, 2026</p>{sections.map(([title,copy])=><section className="mt-9" key={title}><h2 className="text-xl font-black">{title}</h2><p className="mt-3 leading-8 text-slate-600">{copy}</p></section>)}<p className="mt-10 rounded-xl bg-lavender p-4 text-sm text-slate-600">Questions? Email <a className="font-bold text-brand" href="mailto:privacy@intellihub.ai">privacy@intellihub.ai</a>.</p></div></>}
